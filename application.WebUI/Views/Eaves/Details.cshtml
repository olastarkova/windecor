@model application.Domain.Enitites.EavesCollection
@{
    Layout = null;
    
}

<!DOCTYPE html>
<html>
<head>
    @{ Html.RenderPartial("Head"); }
    <meta name="viewport" content="width=device-width" />
    <title>Карнизы серии @Model.Name</title>
    <meta name="Keywords" content="карнизы, карнизы @Model.Name, карнизы @Model.eavesCategoriesEntity.Name, карнизы польша, польские карнизы, карнизы в Минксе, купить карнизы Минск, карнизы Marcin Dekor">
    <meta name="description" content="Карнизы @Model.Name польской фабрики Marcin Dekor" />
</head>
<body> 
    @{ Html.RenderPartial("Header");
    }
    <section class="rolCollections itemDetails content">
        <div class="container">
            <div class="row">
                <div class="col-xs-12">
                    <div class="breadcrumbs">
                        <a href="~/Eaves/Series">Все серии</a>
                    </div>
                </div>
                <div class="col-xs-12 col-md-6">
                    <div class="imageWraper">
                        <img src='~/Content/img/karnizy/@(Model.Id).jpg' alt='"@Model.Name' />
                    </div>
                </div>
                <div class="col-xs-12 col-sm-6">
                    <div class="row">
                        <div class="col-xs-12">
                            <h1>Карнизы серии @Model.Name</h1>
                        </div>
                        <div class="col-xs-12">
                            <p>@Model.Details</p>
                        </div>
                    </div>
                </div>
            </div>
            <div class="row productContent itemList">
             @{Html.RenderAction("EavesItems", "Eaves", new { Model.Id }); }
            </div>
        </div>
    </section>
    @{ Html.RenderPartial("Footer"); }

</body>
</html>
