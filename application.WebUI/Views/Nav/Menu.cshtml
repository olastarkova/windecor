@model IEnumerable<string>

@{ 
    Layout = null;
}

@Html.ActionLink("Все", "List", "Product")


@foreach (var link in Model)
{
    <li>
        @Html.RouteLink(link, new
   {
       controller = "Product",
       action = "List",
       category = link
       //page = 1
   })
@*//new { @class = link == ViewBag.SelectedCategory ? "selected" : null })*@
</li>
}
